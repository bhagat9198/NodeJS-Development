<%- include('../includes/head.ejs') %>
</head>
<body>
  <%- include('../includes/navigation.ejs') %>

  <main class="centered">
    <h1><%= product.title %></h1>
    <hr>
    <div>
      <img class="image" src="<%= product.imageUrl %>" alt="<%= product.title %>">
    </div>
    <h2><%= product.price %></h2>
    <p><%= product.description %></p>
    
    <!-- as "add to cart" code is same for all  3 files (product-details, index, products-list), we have created one include file. thus accessing whenever nessary  -->
    <%- include('../includes/add-to-cart.ejs') %>
  </main>

<%- include('../includes/end.ejs') %>